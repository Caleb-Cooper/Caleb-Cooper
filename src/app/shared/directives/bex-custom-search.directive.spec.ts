import { BexCustomSearchDirective } from './bex-custom-search.directive';

describe('BexCustomSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new BexCustomSearchDirective();
    expect(directive).toBeTruthy();
  });
});
